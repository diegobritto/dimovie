import 'package:flutter/material.dart';

class Colors{
  const Colors();

  static const Color mainColor = const Color.fromRGBO(0, 0, 0, 1);
  static const Color secondColor = const Color.fromRGBO(255,0,0, .5);
  static const Color starsColorUnselected = const Color.fromRGBO(0,0,0, 1);
  static const Color starsColorSelected = const Color.fromRGBO(255,255,0, 1);
  static const Color titleColor =  const Color.fromRGBO(255, 255, 255, .3);
}